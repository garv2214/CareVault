{
  "name": "carevault-blockchain",
  "version": "1.0.0",
  "devDependencies": {
    "hardhat": "^2.22.4",
    "@nomiclabs/hardhat-ethers": "^2.2.3"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "node scripts/deploy.js"
  }
}
