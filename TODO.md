# TODO: Sync Local Changes to CareVault GitHub

Current Progress: Step 1/6 complete (Cleanup)

## Steps:
- [x] 1. Cleanup garbage files (tool artifacts)
- [ ] 2. Stage changes: `git rm --cached -r .` for deletes + `git add` key files/folders
- [ ] 3. Create & switch branch: `git checkout -b blackboxai/sync-local-changes`
- [ ] 4. Final add/commit with detailed message
- [ ] 5. Push: `git push -u origin blackboxai/sync-local-changes`
- [ ] 6. Check gh CLI + optional PR creation

**Next:** Stage git changes. Run `git status` to verify clean.
